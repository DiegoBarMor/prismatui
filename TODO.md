# TODO
- Add a convenient `key_pressed` function that handles the alternative lowercase and uppercase chars.
- Make `utilities/image_formatter.py` into an actual utility.
- Introduce matrix classes for `chars` and `attrs` data (instead of relying on `list[str]` and `list[list[int]]`).
